[project]
name = "damage-demo"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "fpdf2>=2.8.0",
  "python-multipart>=0.0.12",
  "Pillow>=10.0.0",
  "vercel>=0.5.0",
]

[tool.vercel]
entrypoint = "server.main:app"
